<% @puzzles.each do |puzzle| %>
  <div class="card m-3">
    <div class="d-flex">
      <div> <%= cl_image_tag puzzle.photo.attached? ? cl_image_path(puzzle.photo.key) : asset_path('puzzle-placeholder.png'), alt: puzzle.title, height: 200, width: 230 %></div>
      <div class="card-body pl-3">
        <h5 class="card-title mb-4"><%= link_to puzzle.title, puzzle_path(puzzle.id) %></h5>
        <h6 class="card-subtitle"><%= puzzle.size %> Pieces</h6>
        <p class="card-text my-2 text-muted"><i class="fas fa-map-marker-alt"></i> <%= puzzle.address.split.last %></p>
        <p class="card-text"><strong><%= puzzle.price %>CHF</strong>/day</p>
        <%= puzzle.rating %> <i class="fas fa-star" style="color: #FFC65A"></i>
      </div>
    </div>
  </div>
<% end %>
