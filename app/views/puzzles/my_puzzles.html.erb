<div class="container">
  <h1><%= @user.first_name %>'s Puzzles</h1>

  <% @puzzles.each do |puzzle| %>
    <div class="card m-3">
      <div class="d-flex">
        <div> <%= cl_image_tag puzzle.photo.attached? ? cl_image_path(puzzle.photo.key) : asset_path('puzzle-placeholder.jpg'), alt: puzzle.title, height: 200 %></div>
        <div class="card-body pl-3">
          <h5 class="card-title"><%= link_to puzzle.title, puzzle_path(puzzle.id) %></h5>
          <h6 class="card-subtitle mb-2 text-muted"><%= puzzle.size %> Pieces</h6>
          <p class="card-text"><%= puzzle.description %></p>
          <p class="card-text"><%= puzzle.price %> CHF per day</p>
        </div>
      </div>
    </div>
  <% end %>
</div>
